sauna.root = h

logging {
  hipchat {
    room_id = i
    token = j
  }
}

aws {
  access_key_id = c
  secret_access_key = d
  region = b
  dynamodb {
    table_name = e
  }
  sqs {
    queue.name = a
  }
}

optimizely {
  token = f
  import_region = g
}

sendgrid.token = k