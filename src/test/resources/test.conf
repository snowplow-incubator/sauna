queue.name = a
aws {
  access_key_id = b
  secret_access_key = c
  dynamodb {
    table_name = h
  }
}
optimizely {
  token = d
  import_region = i
}
sauna.root = e
logging {
  hipchat {
    room_id = f
    token = g
  }
}